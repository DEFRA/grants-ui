// Records the land parcel chosen by the first-available-parcel step so summary /
// print assertions can reference it via the {SELECTED PARCEL} placeholder when
// the parcel is selected dynamically (rather than hard-coded per identity).
export default { current: null }
