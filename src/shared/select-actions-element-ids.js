// Shared by the page template, its inline loading guard and the client initializer.
export const SELECT_ACTIONS_ELEMENT_IDS = {
  controls: 'select-actions-controls',
  loading: 'select-actions-loading'
}
